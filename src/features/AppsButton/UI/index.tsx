'use client'

import React from 'react'
import { Buttons } from '@/shared/UI/Buttons'

const AppsButton = () => {
    const onClick = () => {}

    return <Buttons.Icon icon='apps' onClick={onClick} />
}

export default AppsButton
